const filtrarUsuarios = (
  usuarios: {
    nome: string;
    idade: number;
    status: boolean;
  }[],
  usuario: string
): {
  nome: string;
  idade: number;
  status: boolean;
}[] => {
  return [];
};
